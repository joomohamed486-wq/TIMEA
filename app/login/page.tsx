 "use client";import {useState} from "react";import {useRouter} from "next/navigation";export default function Login(){const[r,setR]=useState({email:"",password:""});const[msg,setM]=useState("");const router=useRouter();async function go(e:any){e.preventDefault();const x=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(r)});const d=await x.json();if(x.ok)router.push("/account");else setM(d.error)}return <main className="auth"><form onSubmit={go}><h1>تسجيل الدخول</h1><input placeholder="البريد الإلكتروني" type="email" onChange={e=>setR({...r,email:e.target.value})}/><input placeholder="كلمة المرور" type="password" onChange={e=>setR({...r,password:e.target.value})}/>{msg&&<p>{msg}</p>}<button className="btn">دخول</button><a href="/register">إنشاء حساب جديد</a></form></main>}