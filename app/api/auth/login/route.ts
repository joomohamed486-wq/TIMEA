import {NextResponse} from 'next/server';import {createServerSupabaseClient} from '@/lib/supabase-server';import {z} from 'zod';export async function POST(r:Request){const b=z.object({email:z.string().email(),password:z.string()}).parse(await r.json());const s=await createServerSupabaseClient();const {data,error}=await s.auth.signInWithPassword({email:b.email,password:b.password});if(error)return NextResponse.json({error:'بيانات الدخول غير صحيحة'},{status:401});return NextResponse.json({user:data.user})}
