import {NextResponse} from 'next/server';import {createServerSupabaseClient} from '@/lib/supabase-server';import {z} from 'zod';const S=z.object({items:z.array(z.object({productId:z.string().uuid(),quantity:z.number().int().positive()})),shippingAddress:z.record(z.any()),paymentMethod:z.string().default('COD'),discount:z.number().default(0),shipping:z.number().default(0)});export async function POST(r:Request){try{const b=S.parse(await r.json());const s=await createServerSupabaseClient();const {data:{user}}=await s.auth.getUser();if(!user)return NextResponse.json({error:'LOGIN_REQUIRED'},{status:401});const {data:id,error}=await s.rpc('create_order',{p_items:b.items,p_address:b.shippingAddress,p_payment_method:b.paymentMethod,p_discount:b.discount,p_shipping:b.shipping});if(error)return NextResponse.json({error:error.message},{status:400});await s.from('cart_items').delete().in('cart_id',(await s.from('carts').select('id').eq('user_id',user.id)).data?.map((x:any)=>x.id)||[]);return NextResponse.json({id},{status:201})}catch(e:any){return NextResponse.json({error:e.message},{status:400})}}
