'use client';import {useEffect,useState} from 'react';export default function Inventory(){const [rows,setRows]=useState<any[]>([]),[msg,setMsg]=useState('');async function load(){const r=await fetch('/api/admin/inventory');const d=await r.json();setRows(d.rows||[]);if(d.error)setMsg(d.error)}useEffect(()=>{load()},[]);async function adjust(id:string,stock:number){const qty=prompt('المخزون الجديد:',String(stock));if(qty===null)return;const r=await fetch('/api/admin/inventory',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({id,stock:Number(qty),reason:'ADMIN_ADJUSTMENT'})});const d=await r.json();setMsg(r.ok?'تم تحديث المخزون':d.error||'فشل');load()}return <main className="admin"><small>OPERATIONS</small><h1>إدارة المخزون</h1>{msg&&<p className="note">{msg}</p>}<section className="panel"><table className="admin-table"><thead><tr><th>المنتج</th><th>SKU</th><th>المخزون</th><th>حد التنبيه</th><th>الحالة</th><th></th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{x.name}</td><td>{x.sku}</td><td><b>{x.stock}</b></td><td>{x.low_stock_threshold}</td><td>{x.stock<=x.low_stock_threshold?'⚠ منخفض':'✓ جيد'}</td><td><button onClick={()=>adjust(x.id,x.stock)}>تعديل المخزون</button></td></tr>)}</tbody></table></section></main>}
