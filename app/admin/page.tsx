import {createServerSupabaseClient} from '@/lib/supabase-server';import Link from 'next/link';import {redirect} from 'next/navigation';const money=(n:number)=>new Intl.NumberFormat('ar-EG').format(n)+' ج.م';export default async function Admin(){const s=await createServerSupabaseClient();const {data:{user}}=await s.auth.getUser();if(!user)redirect('/login');const {data:p}=await s.from('profiles').select('role').eq('id',user.id).single();if(!p||!['SUPER_ADMIN','ADMIN','STAFF','INVENTORY_MANAGER'].includes(p.role))redirect('/');const [{count:products},{count:users},{data:orders}]=await Promise.all([s.from('products').select('*',{count:'exact',head:true}),s.from('profiles').select('*',{count:'exact',head:true}).eq('role','CUSTOMER'),s.from('orders').select('order_number,total,status,profiles(name)').order('created_at',{ascending:false}).limit(6)]);const sales=(orders||[]).reduce((a:any,o:any)=>a+Number(o.total),0);return <main><header className="top"><Link href="/" className="brand">TIMEA ADMIN</Link><nav><Link href="/">المتجر</Link></nav></header><section className="admin"><small>PRODUCTION ADMIN</small><h1>لوحة التحكم</h1><div className="stats"><div><small>المنتجات</small><b>{products||0}</b></div><div><small>العملاء</small><b>{users||0}</b></div><div><small>آخر الطلبات</small><b>{orders?.length||0}</b></div><div><small>قيمة الطلبات</small><b>{money(sales)}</b></div></div><div className="panel"><h2>آخر الطلبات</h2>{(orders||[]).map((o:any)=><div className="row" key={o.order_number}><span>#{o.order_number} — {o.profiles?.name}</span><b>{money(Number(o.total))}</b><em>{o.status}</em></div>)}</div></section></main>}
