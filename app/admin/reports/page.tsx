import { requireAdmin } from '@/lib/admin-auth';
import { redirect } from 'next/navigation';
export default async function Reports(){const g=await requireAdmin();if(!g.ok)redirect('/login?next=/admin/reports');const [{data:orders},{data:products}]=await Promise.all([g.admin.from('orders').select('status,total,created_at'),g.admin.from('products').select('name,stock,low_stock_threshold,price')]);const total=(orders||[]).reduce((a:any,o:any)=>a+Number(o.total),0);const delivered=(orders||[]).filter(o=>o.status==='DELIVERED').length;const low=(products||[]).filter(p=>p.stock<=p.low_stock_threshold).length;return <main className="admin"><small>ANALYTICS</small><h1>التقارير</h1><div className="stats"><div><small>إجمالي قيمة الطلبات</small><b>{total.toLocaleString('ar-EG')} ج.م</b></div><div><small>الطلبات المسلّمة</small><b>{delivered}</b></div><div><small>منتجات منخفضة المخزون</small><b>{low}</b></div><div><small>عدد المنتجات</small><b>{products?.length||0}</b></div></div><section className="panel"><h2>ملخص الحالات</h2>{['NEW','CONFIRMED','PROCESSING','PACKED','SHIPPED','DELIVERED','CANCELLED','RETURNED','REFUNDED'].map(s=><div className="row" key={s}><span>{s}</span><b>{(orders||[]).filter(o=>o.status===s).length}</b></div>)}</section></main>}
