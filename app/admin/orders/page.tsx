'use client';
import {useEffect,useState} from 'react';
const statuses=['NEW','CONFIRMED','PROCESSING','PACKED','SHIPPED','DELIVERED','CANCELLED','RETURNED','REFUNDED'];
export default function Orders(){const [rows,setRows]=useState<any[]>([]),[selected,setSelected]=useState<any>(null),[msg,setMsg]=useState('');async function load(){const r=await fetch('/api/admin/orders');const d=await r.json();setRows(d.orders||[]);if(d.error)setMsg(d.error)}useEffect(()=>{load()},[]);async function update(id:string,status:string,payment_status:string){const r=await fetch('/api/admin/orders',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({id,status,payment_status})});const d=await r.json();setMsg(r.ok?'تم تحديث الطلب':d.error||'فشل التحديث');load();}return <main className="admin"><small>SALES</small><h1>إدارة الطلبات</h1>{msg&&<p className="note">{msg}</p>}<section className="panel"><div className="table-wrap"><table className="admin-table"><thead><tr><th>رقم الطلب</th><th>العميل</th><th>الإجمالي</th><th>الدفع</th><th>الحالة</th><th>التاريخ</th><th></th></tr></thead><tbody>{rows.map(o=><tr key={o.id}><td>#{o.order_number}</td><td>{o.profiles?.name||o.profiles?.email||o.user_id}</td><td>{Number(o.total).toLocaleString('ar-EG')} ج.م</td><td><select value={o.payment_status} onChange={e=>update(o.id,o.status,e.target.value)}><option>PENDING</option><option>PAID</option><option>FAILED</option><option>REFUNDED</option></select></td><td><select value={o.status} onChange={e=>update(o.id,e.target.value,o.payment_status)}>{statuses.map(s=><option key={s}>{s}</option>)}</select></td><td>{new Date(o.created_at).toLocaleString('ar-EG')}</td><td><button onClick={()=>setSelected(o)}>تفاصيل</button></td></tr>)}</tbody></table></div></section>{selected&&<section className="panel"><button className="close" onClick={()=>setSelected(null)}>×</button><h2>الطلب #{selected.order_number}</h2><pre className="json-box">{JSON.stringify(selected,null,2)}</pre></section>}</main>}
