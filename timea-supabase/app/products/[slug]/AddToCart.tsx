 "use client";import {useState} from "react";import {useRouter} from "next/navigation";export default function AddToCart({productId}:{productId:string}){const[m,setM]=useState("");const r=useRouter();async function add(){const x=await fetch("/api/cart",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({productId,quantity:1})});if(x.ok)r.push("/cart");else setM((await x.json()).error)}return <><button className="btn" onClick={add}>إضافة للسلة</button>{m&&<p>{m}</p>}</>}