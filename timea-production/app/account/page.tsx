import {currentUser} from "@/lib/auth";import {redirect} from "next/navigation";import {prisma} from "@/lib/prisma";import Link from "next/link";export default async function Account(){const u=await currentUser();if(!u)redirect("/login");const orders=await prisma.order.findMany({where:{userId:u.id},orderBy:{createdAt:"desc"},take:20});return <main><header className="top"><Link href="/" className="brand">TIMEA</Link><nav><Link href="/shop">المتجر</Link></nav></header><section className="admin"><small>MY ACCOUNT</small><h1>مرحبًا {u.name}</h1><div className="panel"><h2>طلباتي</h2>{orders.length?orders.map(o=><div className="row" key={o.id}><span>#{o.orderNumber}</span><b>{o.total} ج.م</b><em>{o.status}</em></div>):<p>لا توجد طلبات حتى الآن.</p>}</div></section></main>}